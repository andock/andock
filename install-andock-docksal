sudo mkdir -p /usr/local/bin &&
sudo curl -fsSL https://raw.githubusercontent.com/andock/andock/master/bin/acp-docksal.sh -o /usr/local/bin/acp &&
sudo chmod +x /usr/local/bin/acp
echo "andock wrapper was installed successfully"
